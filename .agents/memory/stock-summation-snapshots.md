---
name: Stock summation = latest snapshot, sum all rows
description: How PlantStock/LipStock totals must be computed (full-snapshot model) to avoid stale/phantom stock and split-row undercounts.
---

Остатки в Sheets (`PlantStock`, `LipStock`) хранятся как СНИМКИ: каждая загрузка
файла = полный снимок склада под датой загрузки (col A). Старые даты остаются
как история. Одна позиция (raw_uid) может идти НЕСКОЛЬКИМИ строками в одном
снимке (разные партии/проценты, напр. «0,21%» и «0,22%»).

**Правило чтения остатков (getLatestPlantStock / getLatestLipStock):**
взять строки ТОЛЬКО максимальной ISO-даты (`/^\d{4}-\d{2}-\d{2}$/`, мусорные/
пустые даты игнорировать), и СУММИРОВАТЬ qty по всем строкам каждого raw_uid в
этом снимке. См. `sumLatestSnapshot()` в sheetsService.ts.

**Why:** старый код делал `map.set(uid, qty)` — last-write-wins по ВСЕМ датам.
Это (1) брало одну строку вместо суммы (Хром 25+12,55 показывал 12,6) и
(2) «всплывали» устаревшие остатки из старых снимков — позиция, которой нет в
последней загрузке, показывала фантомное число (Селен 0 → 50). Позиция,
отсутствующая в последнем снимке = 0 (списана), НЕ берём из истории.

**Запись:**
- `writePlantStock` и `writeLipStockBatch` (загрузка файла /polotsk, /lipkovskaya,
  /kd) — полный снимок: удаляют ВСЕ сегодняшние строки и пишут весь набор. НЕ
  частично по uid (иначе повторная загрузка с меньшим набором оставит мусор).
- `writeLipStock` (ручное обновление ОДНОЙ позиции, POST /api/inventory/lipkovskaya)
  ОБЯЗАН перенести текущий последний снимок в сегодня целиком (carry-forward),
  иначе при чтении «макс. дата» все прочие позиции обнулятся. Переносить ТОЛЬКО
  то, что есть в последнем снимке — списанные позиции не воскрешать.
- У `PlantStock` ручного пути нет (только загрузка файла), поэтому ему
  carry-forward не нужен.

**How to apply:** любую новую функцию агрегации остатков из Sheets строить на
`sumLatestSnapshot`; любой новый путь записи в LipStock держать инвариант
«последняя дата = полный снимок». Округление до 2 знаков: сервер
`Math.round((x+EPSILON)*100)/100`, фронт `Intl.NumberFormat maximumFractionDigits:2`.
`qty_today` в планировании клампится в `Math.max(0, …)` — потребность рецептов
сверх остатка = 0 на руках, дефицит закрывает закупка, не «минус».

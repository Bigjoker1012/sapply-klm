СТОП. НЕ разрешать секрет через GitHub UI.
НЕ делать unblock-secret.
НЕ создавать новый репозиторий.

Обнаруженный GitHub Personal Access Token считать скомпрометированным.

1. Сначала отозвать/удалить этот токен в GitHub и создать новый токен с минимально необходимыми правами.
2. Проверить весь репозиторий на другие секреты:
   - ghp_
   - github_pat_
   - API keys
   - passwords
   - токены в .env и документации.
3. Удалить секрет из истории Git, а не только из текущих файлов.
4. Переписать историю ветки с удалением следующих файлов/секретных данных:
   docs/agentsync_notes.txt
   docs/agentsync_progress.txt
   docs/sync_notes.txt
   docs/sync_progress.txt

5. Перед force-push:
   - сделать резервную копию текущей ветки/clone;
   - сохранить текущий commit SHA;
   - показать план переписывания истории.

6. После очистки:
   - git fsck / проверка истории на наличие старого токена;
   - поиск секретов по всей истории;
   - git status clean;
   - npm build / TypeScript build;
   - убедиться, что рабочий код Supply не изменился.

7. После подтверждения чистой истории выполнить force-push очищенной ветки.

ВАЖНО:
Не менять production Supply на 192.168.51.240.
Не менять БД.
Не менять frontend.
Не менять бизнес-логику.
Только очистка Git-истории и ротация GitHub токена.

В финальном отчёте:
- старый токен отозван;
- новый токен создан;
- какие файлы удалены из истории;
- старый токен больше не находится в git history;
- build успешен;
- новый commit SHA;
- push успешен.
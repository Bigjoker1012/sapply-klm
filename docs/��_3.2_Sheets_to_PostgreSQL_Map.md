# ТЗ 3.2 — Карта Sheets → PostgreSQL

**Дата:** 2026-09-18
**Цель:** документировать маппинг всех Google Sheets на PostgreSQL таблицы

---

## 1. Общая карта

| Google Sheet | PostgreSQL Table | Operations | Статус |
|-------------|-----------------|------------|--------|
| Syryo | sku | R:3 W:1 | ✅ existing (68 rows) |
| Aliases | sku_alias | R:6 W:2 | ⚠️ existing (0 rows, need seed) |
| Analogs | (новая таблица) | R:2 W:1 | ❌ need create |
| Excluded | (новая таблица) | R:3 W:0 | ❌ need create |
| PlantStock | stock_snapshot + stock_item | R:4 W:1 | ⚠️ existing (0 rows, need design) |
| LipStock | stock_snapshot + stock_item | R:5 W:2 | ⚠️ same as PlantStock |
| LipBatches | batch | R:4 W:2 | ⚠️ existing (0 rows, need seed) |
| Inbound | in_transit | R:4 W:2 | ⚠️ existing (0 rows, need seed) |
| Recipes | recipe | R:7 W:4 | ⚠️ existing (0 rows, need seed) |
| RecipeLines | recipe_item | R:6 W:2 | ⚠️ existing (0 rows, need seed) |
| Need | need (новая) | R:2 W:1 | ❌ need create |
| ReviewQueue | upload_row | R:4 W:2 | ⚠️ existing (0 rows) |
| AppLog | event_log | R:0 W:0 | ⚠️ existing (0 rows, minimal use) |

---

## 2. Детальный маппинг по сущностям

### 2.1 Справочник сырья: Syryo → sku

**Sheets: Syryo**
- Колонки: A=raw_uid, B=full_name, C=short_name, D=unit, E=active, F=category, G=default_supplier, H=min_stock_kg, I=reorder_point_kg, J=shelf_life_days

**PG: sku** (существует, 68 rows)
| Поле Syryo | Поле PG | Тип | Примечание |
|-----------|---------|-----|-----------|
| A (raw_uid) | code | text | raw_uid = sku.code |
| B (full_name) | name | text | |
| C (short_name) | — | — | нет в PG, добавить |
| D (unit) | unit | text | default 'кг' |
| E (active) | active | boolean | |
| F (category) | category | text enum | |
| G (default_supplier) | defaultSupplierId | FK → supplier | |
| H (min_stock_kg) | minStockKg | double | |
| I (reorder_point_kg) | reorderPointKg | double | |
| J (shelf_life_days) | shelfLifeDays | integer | |

**Нужно добавить в sku:** short_name (text)
**Действие:** ALTER TABLE + миграция данных из Syryo

### 2.2 Синонимы: Aliases → sku_alias

**Sheets: Aliases**
- Колонки: A=id, B=raw_uid (canonical), C=alias, D=source

**PG: sku_alias** (существует, 0 rows)
| Поле Aliases | Поле PG | Тип | Примечание |
|-------------|---------|-----|-----------|
| A (id) | id | serial | auto-generated |
| B (raw_uid) | skuId | FK → sku | через code lookup |
| C (alias) | alias | text | unique index на lower(alias) |
| D (source) | source | text enum | '1c'/'pdf_recipe'/'manual' |

**Действие:** seed из Aliases, связать через sku.code

### 2.3 Остатки: PlantStock + LipStock → stock_snapshot + stock_item

**Sheets: PlantStock**
- Колонки: A=snapshot_date, B=raw_uid, C=name, D=qty, E=unit, F=source_file, G=deleted

**Sheets: LipStock**
- Колонки: A=snapshot_date, B=raw_uid, C=name, D=total_qty, E=reserved_qty, F=free_qty, G=unit, H=source, I=deleted

**PG: stock_snapshot** (существует, 0 rows)
| Поле Sheets | Поле PG | Тип | Примечание |
|------------|---------|-----|-----------|
| A (snapshot_date) | snapshotDate | text | YYYY-MM-DD |
| F/H (source) | source | text | 'upload_job:X' или 'manual' |
| — | warehouseId | FK → warehouse | 1=Полоцк, 2=Липковская |
| — | payloadJson | jsonb | [{raw_uid, qty}] |

**Нужно:** отдельная таблица stock_item для строк снимка
| Поле | Тип | Примечание |
|------|-----|-----------|
| id | serial PK | |
| snapshotId | FK → stock_snapshot | |
| raw_uid | text | или skuId FK |
| quantity | double | |
| unit | text | 'кг' |

**Действие:** redesign stock_snapshot, create stock_item

### 2.4 Партии: LipBatches → batch

**Sheets: LipBatches**
- Колонки: A=snapshot_date, B=raw_uid, C=batch_code, D=vendor_name, E=qty, F=unit, G=source, H=expiry_date, I=manufacture_date

**PG: batch** (существует, 0 rows)
| Поле LipBatches | Поле PG | Тип | Примечание |
|----------------|---------|-----|-----------|
| B (raw_uid) | skuId | FK → sku | через code lookup |
| C (batch_code) | lotNo | text | |
| D (vendor_name) | — | — | нет в PG, добавить? |
| E (qty) | initialQtyKg | double | |
| H (expiry_date) | expiryDate | text | |
| I (manufacture_date) | manufactureDate | text | |
| — | warehouseId | FK → warehouse | 2=Липковская |
| — | currentQtyKg | double | = initialQtyKg |
| — | status | text enum | 'active' |

**Нужно добавить в batch:** vendor_name (text)
**Действие:** ALTER TABLE + миграция данных

### 2.5 Рецепты: Recipes + RecipeLines → recipe + recipe_item

**Sheets: Recipes**
- Колонки: A=recipe_uid, B=code, C=full_name, D=premix_name, E=date, F=concentration, G=batch_t, H=customer, I=period, J=quarter, K=file_name, L=status, M=base_batch_kg

**PG: recipe** (существует, 0 rows)
| Поле Recipes | Поле PG | Тип | Примечание |
|-------------|---------|-----|-----------|
| A (recipe_uid) | — | — | нет в PG, добавить! |
| B (code) | code | text | |
| C (full_name) | name | text | |
| D (premix_name) | — | — | нет в PG |
| E (date) | activeFrom | text | |
| G (batch_t) | — | — | нет в PG, добавить |
| L (status) | status | text enum | maps: план→active, отменён→archived |
| — | targetAnimal | text enum | default 'other' |
| — | version | integer | default 1 |

**Нужно добавить в recipe:** recipe_uid (text, unique), batch_t (double), base_batch_kg (double)

**Sheets: RecipeLines**
- Колонки: A=line_id, B=recipe_uid, C=raw_uid, D=name_from_recipe, E=activity, F=input_pct, G=norm_g_per_t, H=consumption_kg, I=price_per_kg, J=cost_per_t, K=is_total, L=match_status

**PG: recipe_item** (существует, 0 rows)
| Поле RecipeLines | Поле PG | Тип | Примечание |
|-----------------|---------|-----|-----------|
| B (recipe_uid) | recipeId | FK → recipe | через recipe_uid lookup |
| C (raw_uid) | skuId | FK → sku | через code lookup |
| F (input_pct) | doseKgPerT | double | |
| G (norm_g_per_t) | — | — | нет в PG |
| H (consumption_kg) | — | — | нет в PG, расчёт на лету |
| L (match_status) | — | — | нет в PG |

**Нужно добавить в recipe_item:** norm_g_per_t (double), consumption_kg (double), match_status (text)

### 2.6 Потребность: Need → need (НОВАЯ таблица)

**Sheets: Need**
- Колонки: A=need_uid, B=recipe_uid, C=raw_uid, D=period, E=gross_qty, F=adjustment, G=net_qty, H=version

**PG: need** (НЕТ таблицы)
| Поле Need | Поле PG | Тип | Примечание |
|----------|---------|-----|-----------|
| A (need_uid) | id | serial PK | |
| B (recipe_uid) | recipeId | FK → recipe | |
| C (raw_uid) | skuId | FK → sku | |
| D (period) | period | text | 'YYYY-MM' |
| E (gross_qty) | grossQty | double | |
| F (adjustment) | adjustment | double | default 0 |
| G (net_qty) | netQty | double | |
| H (version) | version | integer | |

**Действие:** CREATE TABLE need

### 2.7 Транзит: Inbound → in_transit

**Sheets: Inbound**
- Колонки: A=inbound_uid, B=raw_uid, C=raw_name, D=qty, E=eta, F=destination, G=status, H=supplier, I=po_ref, J=created_at, K=deleted

**PG: in_transit** (существует, 0 rows)
| Поле Inbound | Поле PG | Тип | Примечание |
|-------------|---------|-----|-----------|
| A (inbound_uid) | id | serial PK | |
| B (raw_uid) | skuId | FK → sku | через code lookup |
| D (qty) | qtyKg | double | |
| E (eta) | etaDate | text | YYYY-MM-DD |
| F (destination) | warehouseId | FK → warehouse | lookup by name |
| G (status) | status | text enum | |
| H (supplier) | supplierId | FK → supplier | lookup by name |
| I (poRef) | poRef | text | |
| J (created_at) | createdAt | text | |

**Действие:** seed из Inbound

### 2.8 Очередь распознавания: ReviewQueue → upload_row

**Sheets: ReviewQueue**
- Колонки: A=id, B=text, C=source_type, D=file_name, E=qty, F=source_warehouse, G=resolved, H=resolved_by, I=resolved_at

**PG: upload_row** (существует, 0 rows)
| Поле ReviewQueue | Поле PG | Тип | Примечание |
|-----------------|---------|-----|-----------|
| A (id) | id | serial PK | |
| B (text) | rawPayload | jsonb | {name: text} |
| C (source_type) | — | — | нет в PG |
| D (file_name) | — | — | нет в PG |
| G (resolved) | action | text enum | resolved→'auto_apply' |
| H (resolved_by) | reviewedBy | FK → user | |

**Проблема:** upload_row привязан к upload_job, а ReviewQueue — нет. Нужно либо:
1. Создать upload_job для каждого файла ReviewQueue
2. Или создать отдельную таблицу unresolved_item

**Решение:** создать таблицу `unresolved_item` (проще, чем связывать с upload_job)

### 2.9 Исключения: Excluded → excluded_item (НОВАЯ таблица)

**Sheets: Excluded**
- Колонки: A=id, B=text, C=source_type, D=created_at

**PG: НЕТ таблицы**
| Поле Excluded | Поле PG | Тип | Примечание |
|--------------|---------|-----|-----------|
| A (id) | id | serial PK | |
| B (text) | text | text | |
| C (source_type) | sourceType | text | |
| D (created_at) | createdAt | text | |

**Действие:** CREATE TABLE excluded_item

### 2.10 Аналоги: Analogs → analog (НОВАЯ таблица)

**Sheets: Analogs**
- Колонки: A=id, B=raw_uid, C=analog_raw_uid, D=note, E=created_at

**PG: НЕТ таблицы**
| Поле Analogs | Поле PG | Тип | Примечание |
|-------------|---------|-----|-----------|
| A (id) | id | serial PK | |
| B (raw_uid) | skuId | FK → sku | |
| C (analog_raw_uid) | analogSkuId | FK → sku | |
| D (note) | note | text | |
| E (created_at) | createdAt | text | |

**Действие:** CREATE TABLE analog

### 2.11 Настройки планирования: purchase_plan_setting

**PG: purchase_plan_setting** (существует, 61 row)
- Уже в PG, используется в planning.ts
- Поля: skuCode, coefficient, manualInput, manualAvgUsage

**Действие:** использовать как есть

### 2.12 Архив документов: document_archive

**PG: document_archive** (существует, 137 rows)
- Уже в PG, используется для хранения рецептов

**Действие:** использовать как есть

---

## 3. Новые таблицы (создать)

| Таблица | Описание |
|---------|----------|
| analog | Связи аналогов между SKU |
| excluded_item | Исключённые из распознавания позиции |
| unresolved_item | Очередь нераспознанных позиций |
| need | Потребность по рецептам |

---

## 4. Таблицы для изменения (ALTER TABLE)

| Таблица | Изменение |
|---------|----------|
| sku | ADD short_name TEXT |
| batch | ADD vendor_name TEXT |
| recipe | ADD recipe_uid TEXT UNIQUE, ADD batch_t DOUBLE, ADD base_batch_kg DOUBLE |
| recipe_item | ADD norm_g_per_t DOUBLE, ADD consumption_kg DOUBLE, ADD match_status TEXT |

---

## 5. Таблицы переиспользуемые как есть

| Таблица | Статус |
|---------|--------|
| organization | ✅ OK |
| warehouse | ✅ OK |
| supplier | ✅ OK |
| user | ✅ OK |
| session | ✅ OK |
| upload_job | ✅ OK |
| document_archive | ✅ OK |
| purchase_plan_setting | ✅ OK |
| event_log | ✅ OK (минимальное использование) |

---

## 6. Таблицы пустые (нужен seed)

| Таблица | Источник seed |
|---------|--------------|
| sku | Syryo (68 rows) |
| sku_alias | Aliases |
| batch | LipBatches |
| recipe | Recipes |
| recipe_item | RecipeLines |
| in_transit | Inbound |
| upload_row | ReviewQueue (через unresolved_item) |

---

## 7. Схема связей

```
organization ──< warehouse
                │
                ├──< batch ──< stockMovement
                │     │
                │     └──< transferBatch ──< transfer
                │
                └──< productionPlan ──< recipe ──< recipeItem ──< sku
                                                          │
                                                          └──< skuAlias
                                                          └──< analog
                                                          └──< batch
                                                          └──< inTransit
                                                          └──< purchaseOrder
                                                          
sku ──< need ──< recipe

warehouse ──< stockSnapshot ──< stockItem ──< sku

uploadJob ──< uploadRow ──< sku
unresolvedItem (отдельная)
excludedItem (отдельная)
```
